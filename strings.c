#include <stdio.h>
#include <string.h>

int main()
{
    char s[]="i want to pass";
    int i;
    for(i=0;s[i]!='\0';i++)	
    {
	;
    }
    
    printf("%d\n",sizeof(double *));
    printf("the string has %d characters\n",i);

	return 0;
}